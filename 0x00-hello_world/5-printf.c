#include <stdio.h>

/**
 * main - print a strin using the printf function
 *
 * Description: A program that prints exactly "with proper grammar,
 * but the outcome is a piece of art"
 *
 * Return: 0 for succcess
 *
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
