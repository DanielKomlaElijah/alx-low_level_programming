#include "main.h"

/**
 * _strlen - Function that returns the
 * length of a string
 * @s: first arguement for the function
 * Return: 0
 */
int _strlen(char *s)
{
	int n;

	for (n = 0; s[n] != 0; n++)
	{
	}
	return (n);
}
